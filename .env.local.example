# Copiar a .env.local para desarrollo local
BACKEND_URL=http://localhost:8080

# ── Vercel (Dashboard → Settings → Environment Variables) ──
# BACKEND_URL=https://appstripe-api.onrender.com
# Obligatorio antes del build: next.config.ts usa esta variable para el proxy /backend/*
